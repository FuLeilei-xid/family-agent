# 《家邻智选——面向家庭终端的本地生活决策与服务执行智能体》比赛文档

------

## 1. 项目概述

### 1.1 项目名称

家邻智选——面向家庭终端的本地生活决策与服务执行智能体

### 1.2 项目简介

本项目基于当前已有的 `ai-agent-scaffold-lite` 多模块工程，面向家庭终端场景，构建一个可在轻量化算力环境下运行的本地生活智能体。系统能够理解家庭成员的自然语言需求，完成意图识别、槽位提取、任务规划、工具调用、多轮对话管理和结果生成，为家庭成员提供餐饮推荐、周边服务决策、优惠选择和连续会话服务。

项目强调：

- 端侧友好：采用轻量理解与结构化任务执行，降低模型推理负担
- 真实落地：基于真实店铺查询、详情查询、优惠券查询工具链
- 多轮智能：支持上下文保持、条件补充、连续追问
- 隐私优先：家庭偏好、用户状态尽量本地化处理
- 高复用性：基于当前 Spring Boot + Agent 脚手架快速扩展

------

## 2. 场景选择与痛点分析

### 2.1 选择场景

本项目选择的比赛场景为：

家庭场景——家庭本地生活决策与服务智能体

### 2.2 目标用户

- 家庭中的父母
- 老人
- 儿童陪同场景
- 需要快速完成“吃什么、去哪儿、怎么选、是否划算”决策的普通家庭用户

### 2.3 典型场景

- 今晚一家三口出去吃，老人不能吃辣，小孩想吃面，预算 100 元以内，推荐附近还在营业的店
- 想找适合带老人去的清淡餐馆，并比较哪家更划算
- 继续追问“第二家店营业到几点”“这家有券吗”“还有别的吗”
- 周末家庭聚餐，结合评分、距离、优惠和口味做综合推荐

### 2.4 核心痛点

#### 痛点 1：家庭决策约束多

家庭成员偏好不同，往往存在：

- 老人清淡、不吃辣
- 小孩偏爱面食、环境敏感
- 家长关注预算、距离、效率

传统搜索只能返回列表，无法做综合决策。

#### 痛点 2：真实需求往往是多轮的

用户第一次提问往往不完整，需要：

- 继续 уточ问预算
- 补充距离要求
- 确认营业状态
- 追问详情与优惠

普通问答系统缺乏上下文保持能力。

#### 痛点 3：仅靠大模型生成不可靠

如果没有真实工具支持，模型容易：

- 编造店铺
- 编造地址和优惠
- 无法基于真实信息比较

所以必须引入工具调用和结构化执行。

#### 痛点 4：端侧算力有限

比赛限制设备通常在：

- 不超过 6 TOPS
- 4GB 内存以内

因此不能依赖重型云端大模型方案，需要轻量理解、规划和执行。

------

## 3. 创新点

### 3.1 家庭画像驱动的本地生活智能体

区别于普通导购助手，本项目不仅做“找店”，更做“家庭适配推荐”，让推荐结果考虑：

- 老人饮食限制
- 儿童偏好
- 家庭预算
- 距离与营业状态
- 优惠力度

### 3.2 轻量化 Agent 架构

采用“轻量意图理解 + 工具链执行 + 结果重排 + 多轮记忆”的架构，避免把所有问题都交给大模型生成，适合端侧场景。

### 3.3 工具驱动的真实决策链路

系统可调用真实业务工具完成：

- 店铺搜索
- 条件筛选
- 店铺详情查询
- 优惠券查询

最终输出的是有依据的推荐结果，不是纯生成式答案。

### 3.4 多轮状态保持

支持用户在上一轮推荐基础上继续追问：

- “第二家呢？”
- “这家有优惠券吗？”
- “换成明天中午”
- “距离再近一点”

实现连续任务而非单轮问答。

### 3.5 面向端侧的算力优化

通过：

- 规则约束
- 模板化输出
- 工具优先
- 局部模型推理
- 轻量缓存

降低响应时延与资源消耗。

------

## 4. 系统总体架构

## 4.1 总体架构说明

系统整体采用当前项目已有的分层与多模块结构，形成如下链路：

前端交互层 → 后端接口层 → Agent 编排层 → 工具调用层 → 业务数据层 → 响应生成层

### 4.2 业务链路

用户输入家庭需求

​    ↓

前端采集文本/语音请求

​    ↓

后端接口接收消息并创建/复用会话

​    ↓

Agent 进行意图识别、槽位抽取、任务规划

​    ↓

调用店铺搜索/筛选/详情/优惠券工具

​    ↓

对结果进行家庭适配排序

​    ↓

生成推荐结果并返回前端

​    ↓

用户继续追问，系统保持会话状态

### 4.3 架构分层

#### 1）前端层

负责：

- 聊天界面
- 会话管理
- 流式消息展示
- 推荐结果卡片展示
- 用户画像设置入口

#### 2）触发层

负责：

- HTTP API 暴露
- SSE 流式输出
- 参数校验
- 错误处理

#### 3）Agent 层

负责：

- 智能体配置装配
- 会话创建与维护
- 多轮上下文保持
- 工具调用与结果整合

#### 4）领域层

负责：

- 对话服务抽象
- 任务执行与会话管理
- 业务语义封装

#### 5）基础设施层

负责：

- 工具适配
- 认证拦截
- 外部服务连接
- 配置管理

#### 6）数据与缓存层

负责：

- 会话状态缓存
- 家庭画像缓存
- 店铺数据查询结果缓存
- 可选的本地知识库索引

------

## 5. 基于当前项目的技术栈

本项目必须基于当前仓库进行实现，因此技术栈需与现有工程保持一致。

### 5.1 后端技术栈

- Java 17
- Spring Boot 3.4.3
- Spring Web
- Spring Validation
- Spring AI 1.1.0-M3
- Google ADK 0.5.0
- LangChain4j 1.4.0
- MCP Client（WebFlux Starter）
- Fastjson
- Guava
- Lombok
- MySQL Connector
- Maven 多模块工程

### 5.2 Agent 技术栈

- Google ADK
- Spring AI
- LangChain4j
- MCP 工具调用机制
- YAML 智能体配置
- 多 Agent / 单 Agent 编排能力

### 5.3 前端技术栈

基于当前项目已有内容，前端建议采用：

- HTML5
- CSS3
- 原生 JavaScript
- SSE（Server-Sent Events）流式输出
- Nginx 静态页面部署

说明：
当前仓库 `docs/nginx/html/` 已有静态页面基础，因此最契合比赛的做法不是新增重型前端框架，而是用轻量化 Web 前端快速完成可演示 Demo，这也更符合“低成本、低资源、可部署”的比赛导向。

------

## 6. 项目现有基础与可复用能力

### 6.1 当前已有工程结构

项目当前已具备多模块结构：

- `ai-agent-scaffold-lite-app`：应用启动与配置
- `ai-agent-scaffold-lite-trigger`：HTTP 接口入口
- `ai-agent-scaffold-lite-domain`：领域服务与对话服务
- `ai-agent-scaffold-lite-infrastructure`：基础设施与工具适配
- `ai-agent-scaffold-lite-api`：接口 DTO 与服务定义
- `ai-agent-scaffold-lite-types`：公共类型与异常定义

### 6.2 当前已有智能导购工具能力

当前项目已经具备以下工具能力：

- `shopSearch`
- `shopSearchAdvanced`
- `shopDetail`
- `voucherList`

这为比赛场景中的“家庭本地生活决策”提供了直接能力基础。

### 6.3 当前已有对话接口能力

项目已具备：

- 普通对话接口
- 流式对话接口
- 会话创建与管理接口

### 6.4 当前已有前端基础

仓库已有 `docs/nginx/html/index.html` 静态页面，可直接扩展为比赛 Demo 页面。

------

## 7. 前端需要实现的内容

比赛 Demo 必须可视化，因此前端是必做项。

## 7.1 前端目标

构建一个轻量化家庭智能体交互界面，支持：

- 文本输入
- 多轮对话显示
- 流式回复
- 家庭条件选择
- 推荐结果可视化
- 会话状态展示

## 7.2 前端页面设计

### 页面 1：家庭智能体主聊天页

功能包括：

- 聊天消息区
- 输入框
- 发送按钮
- 流式输出区域
- 当前会话 ID 展示
- 当前家庭画像摘要展示

### 页面 2：家庭画像设置页

可设置：

- 家庭人数
- 老人是否在场
- 儿童是否在场
- 饮食偏好
- 默认预算区间
- 默认活动半径
- 常去区域

### 页面 3：推荐结果卡片

每个店铺卡片展示：

- 店铺名称
- 评分
- 人均消费
- 距离/区域
- 营业状态
- 是否有优惠
- 推荐理由

### 页面 4：演示模式页

用于视频录制，内置示例问题：

- 家庭晚餐推荐
- 老人友好餐厅推荐
- 儿童友好餐厅推荐
- 优惠券对比推荐

## 7.3 前端实现内容清单

### 必做

- 对接 `/api/shopping-guide/chat`
- 对接 `/api/shopping-guide/chat/stream`
- 管理 `sessionId`
- 消息气泡展示
- 推荐结果卡片化渲染
- 支持“继续追问”

### 加分项

- 家庭画像面板
- 常用问题快捷按钮
- 结果排序说明
- 对话过程中的“正在规划/正在查询/正在比较”状态提示

## 7.4 前端性能设计

- 原生 JS 实现，减少框架开销
- SSE 按 token/片段增量渲染
- 缓存最近会话消息
- 静态资源由 Nginx 直接托管

------

## 8. 后端需要实现的内容

## 8.1 后端目标

基于现有 Spring Boot 多模块项目，完成一个面向家庭场景的智能体后端服务。

## 8.2 后端需要新增/完善的能力

### 1）家庭场景专用接口

建议新增：

- `POST /api/family/profile/save`：保存家庭画像
- `GET /api/family/profile/query`：查询家庭画像
- `POST /api/family/recommend/chat`：家庭智能体对话
- `GET /api/family/recommend/chat/stream`：家庭智能体流式对话

### 2）会话上下文增强

当前已有会话机制，需要扩展保存：

- 家庭画像摘要
- 当前候选店铺列表
- 最近一次推荐结果
- 已识别槽位信息
- 用户最近补充条件

### 3）任务路由与结果重排

新增后端逻辑：

- 根据用户输入识别任务类型
- 根据任务类型路由不同工具组合
- 对候选店铺进行二次排序与推荐理由生成

### 4）本地缓存

可以引入：

- 家庭画像缓存
- 对话上下文缓存
- 最近查询结果缓存
- 热门店铺结果缓存

## 8.3 后端模块分工建议

### `ai-agent-scaffold-lite-trigger`

负责：

- Controller 编写
- 请求参数接收
- SSE 输出
- 跨域和异常处理

### `ai-agent-scaffold-lite-domain`

负责：

- 家庭画像实体
- 会话上下文实体
- 任务规划服务
- 推荐排序服务
- 多轮状态管理服务

### `ai-agent-scaffold-lite-infrastructure`

负责：

- 黑马点评/本地生活服务调用适配
- 缓存实现
- 外部接口认证
- 可选的本地知识库适配

### `ai-agent-scaffold-lite-api`

负责：

- 请求 DTO
- 响应 DTO
- 服务接口定义

### `ai-agent-scaffold-lite-app`

负责：

- 应用启动
- Agent 自动装配
- 配置加载
- 线程池和通用组件装配

## 8.4 后端建议新增数据结构

### 家庭画像 DTO

{

  "userId": 1001,

  "familySize": 3,

  "hasElderly": true,

  "hasChild": true,

  "elderlyPreference": "清淡",

  "childPreference": "面食",

  "budgetRange": "50-120",

  "defaultArea": "西湖",

  "distancePreference": "近"

}

### 会话上下文 DTO

{

  "sessionId": "xxx",

  "currentIntent": "restaurant_recommendation",

  "slots": {

​    "budgetMax": 120,

​    "openNow": true,

​    "minScore": 4

  },

  "candidateShopIds": [1, 2, 3],

  "lastRecommendShopId": 2

}

------

## 9. Agent 需要实现的内容

## 9.1 Agent 目标

构建一个面向家庭本地生活场景的智能体，具备：

- 意图理解
- 槽位填充
- 任务规划
- 工具调用
- 多轮会话保持
- 结果解释生成

## 9.2 Agent 核心职责

### 1）识别用户意图

意图包括但不限于：

- 餐饮推荐
- 店铺搜索
- 店铺详情查询
- 优惠券查询
- 结果比较
- 修改条件
- 基于上一轮追问

### 2）提取关键槽位

槽位包括：

- 店铺名称
- 店铺类型
- 区域
- 人均预算
- 最低评分
- 是否营业中
- 家庭成员限制
- 距离偏好
- 时间条件

### 3）任务规划

根据槽位决定调用工具链：

- 模糊找店：`shopSearch`
- 条件推荐：`shopSearchAdvanced`
- 详情查询：`shopDetail`
- 优惠查询：`voucherList`

### 4）上下文保持

当用户说：

- “这家店怎么样”
- “第二家有券吗”
- “换成明天中午”
- “还有更便宜的吗”

Agent 必须能继承上一轮状态。

### 5）结果解释

Agent 不只是返回结果，更要说明推荐理由：

- 为什么推荐这家
- 哪家更适合老人
- 哪家更适合孩子
- 哪家更划算

## 9.3 Agent 设计原则

- 工具结果优先，降低幻觉
- 结构化执行优先，减少冗余生成
- 家庭画像优先，提高推荐可信度
- 多轮上下文优先，体现智能体价值

## 9.4 Agent 提示词设计方向

建议将当前“智能导购助手”升级为“家庭生活智能体”，提示词中加入：

- 家庭成员画像感知
- 多轮引用最近店铺列表
- 当信息不足时主动追问
- 优先给出 3~5 个代表性选项
- 强调基于工具结果回答
- 不得编造优惠和店铺信息

## 9.5 Agent 工作流

输入用户问题

​    ↓

解析是否为家庭生活相关请求

​    ↓

识别意图与槽位

​    ↓

读取家庭画像与历史上下文

​    ↓

生成任务规划

​    ↓

执行工具调用

​    ↓

结果过滤与排序

​    ↓

生成自然语言回复

​    ↓

更新会话状态

------

## 10. AI 模型与算法说明

## 10.1 模型选择原则

由于比赛要求在低算力环境下落地，因此本项目不追求大模型参数规模，而强调：

- 小模型负责理解与分类
- 工具负责查真实数据
- 模板或轻量生成负责输出

## 10.2 推荐采用的算法策略

### 1）轻量意图识别

采用：

- 小型文本分类模型
- 或规则 + 小模型混合路由

### 2）槽位抽取

采用：

- 规则模板抽取
- 关键词词典匹配
- 小模型补充抽取

### 3）家庭适配排序

对候选店铺建立评分函数，考虑：

- 评分
- 均价是否落预算内
- 是否营业
- 是否有优惠
- 是否适合老人/儿童
- 区域与距离匹配

### 4）结果生成

采用两级生成机制：

- 一级：结构化模板快速生成
- 二级：轻量 LLM 做润色与解释

------

## 11. 本地知识库与 RAG 设计

## 11.1 是否必须做 RAG

本项目不是纯知识问答，更偏任务执行，因此不将 RAG 作为核心主链路。

## 11.2 可选轻量 RAG 方向

为了贴合比赛方向，可加入轻量本地知识库：

- 家庭常见饮食偏好标签
- 店铺类型标签
- 老人饮食建议词典
- 儿童友好环境标签
- 小区周边常用 POI 索引

## 11.3 使用方式

RAG 只做辅助：

- 用于补充推荐理由
- 用于偏好标签匹配
- 用于场景词语归一化
- 不替代核心工具查询

------

## 12. 性能优化措施

## 12.1 端侧友好优化

- 使用轻量化意图识别而非全量大模型理解
- 优先调用工具而不是长文本自由生成
- 使用结构化模板减少推理 token 消耗
- 常用家庭画像本地缓存
- 常用查询结果短时缓存

## 12.2 并发与响应优化

- 使用线程池处理异步任务
- SSE 流式输出提升交互体验
- 热门条件查询结果缓存
- 详情查询与优惠查询可并行化

## 12.3 工程优化

- 模块化拆分便于裁剪部署
- 静态前端部署成本低
- 后端单服务即可完成比赛 Demo 演示

------

## 13. 隐私与安全设计

- 家庭画像优先本地或本系统内缓存，不外传敏感信息
- 用户身份与会话分离管理
- 仅在必要时请求外部服务
- 不上传非必要原始家庭数据
- 对敏感偏好信息进行最小化存储

------

## 14. 系统可扩展性

本项目不仅适用于家庭场景，也可迁移至：

- 校园周边生活服务助手
- 社区便民助手
- 中小企业园区生活服务助手

扩展方向包括：

- 接入地图导航工具
- 接入预约下单工具
- 接入语音唤醒
- 接入视觉识别
- 支持更多生活服务类型

------

## 15. 演示方案设计

## 15.1 演示目标

通过 3 分钟以内视频展示：

- 自然语言输入
- 智能体理解需求
- 工具调用能力
- 多轮连续对话
- 推荐结果解释
- 低延迟交互体验

## 15.2 推荐演示脚本

### 场景 1：家庭晚餐推荐

用户输入：

> 今晚一家三口出去吃，老人不能吃辣，小孩想吃面，预算 100 以内，找一家近一点、还在营业的店。

系统展示：

- 识别需求
- 检索候选店铺
- 返回推荐列表
- 给出推荐理由

### 场景 2：继续追问

用户继续输入：

> 第二家店营业到几点？有优惠券吗？

系统展示：

- 继承上一轮店铺列表
- 查询第二家店详情
- 查询优惠券
- 返回精确结果

### 场景 3：条件修改

用户继续输入：

> 那换成明天中午，想找环境更适合带孩子的。

系统展示：

- 保留预算与家庭画像
- 更新时间与偏好
- 重新推荐

------

## 16. 项目最终实现清单

## 16.1 前端实现清单

-  家庭智能体聊天页
-  SSE 流式消息显示
-  会话保持
-  家庭画像设置面板
-  推荐卡片展示
-  演示预设问题快捷入口

## 16.2 后端实现清单

-  家庭画像接口
-  家庭推荐会话接口
-  会话上下文增强
-  推荐排序逻辑
-  缓存机制
-  推荐理由生成逻辑

## 16.3 Agent 实现清单

-  家庭场景 Agent 提示词重构
-  意图与槽位定义
-  多轮上下文策略
-  工具路由策略
-  结果排序策略
-  追问策略

------

## 17. 项目目录建议

比赛提交时建议整理为如下结构：

团队名-学校

├── algorithm

│   ├── ai-agent-scaffold-lite-api

│   ├── ai-agent-scaffold-lite-app

│   ├── ai-agent-scaffold-lite-domain

│   ├── ai-agent-scaffold-lite-infrastructure

│   ├── ai-agent-scaffold-lite-trigger

│   ├── ai-agent-scaffold-lite-types

│   ├── docs

│   ├── deploy.sh

│   ├── test.sh

│   └── 代码说明文档.pdf

├── data

│   ├── 创新方案设计文档.pdf

│   └── 演示视频.mp4

------

## 18. 项目总结

“家邻智选”不是一个简单的聊天机器人，而是一个面向家庭场景的轻量化服务智能体。它依托当前项目已有的多模块 Java 架构、Agent 配置体系、工具调用能力和基础前端页面，能够在有限算力环境下实现真实场景中的智能服务闭环。

本项目的核心价值在于：

- 真实场景：解决家庭日常高频决策问题
- 真实能力：依托工具调用查询真实数据
- 真实智能：支持多轮追问与状态保持
- 真实落地：适配轻量环境，易部署、低成本
- 真实扩展：可向社区、校园、企业等场景延伸

最终，本项目体现了比赛所倡导的“小模型、大智慧”理念：
在算力受限的环境下，不依赖超大模型堆砌，而通过轻量理解、工具编排、状态管理和场景设计，实现高效、智能、可落地的端侧 Agent 服务